players = {}
# extract data from shots_data.csv
with open('shots_data.csv', 'r') as file:
    csvreader = csv.reader(file)
    header = next(csvreader)
    for row in csvreader:
        if row[0] in players:
            kwopkepd
        else:
            players[row[0]] = 
        if(row[0] ==)
        if(row[0] == "Bradley Beal"): # organize by Player
            teamA.addShot(row)
        elif(row[0] == "John Wall"):    
            teamB.addShot(row)